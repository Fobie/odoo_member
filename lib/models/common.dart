enum ImageType { profile, signup }

enum MemberSearchType { phone, barcode }

class AttributeDisplayType {
  static const String color = 'color';
  static const String select = 'select';
  static const String radio = 'radio';
}
